namespace RYHME.Models;

public enum ReleaseStatus
{
    Draft,
    Scheduled,
    Released,
    Cancelled
}

public enum UserRole
{
    Admin,
    Manager
}

public enum ReleaseType
{
    Single,
    Album
}
